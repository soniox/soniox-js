/**
 * Common types used across the SDK
 */

/** Supported languages (ISO 639-1 codes) */
export type LanguageCode = string;

/** Configuration options for the Soniox client */
export interface SonioxClientConfig {}
