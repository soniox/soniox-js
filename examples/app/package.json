{
  "name": "soniox-example-app",
  "version": "1.0.0",
  "description": "Soniox SDK example app",
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "dev:server": "tsx --watch --env-file=.env server.ts",
    "dev:web": "vite",
    "build": "vite build",
    "start": "npm run build && tsx --env-file=.env server.ts"
  },
  "dependencies": {
    "@soniox/node": "*",
    "express": "^5.2.1",
    "preact": "^10.19.3",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.4",
    "@types/express": "^5.0.6",
    "@types/ws": "^8.18.1",
    "tsx": "^4.21.0",
    "vite": "^6.1.0"
  }
}
