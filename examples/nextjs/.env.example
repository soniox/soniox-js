SONIOX_API_KEY=<SONIOX_API_KEY>

# Optional: regional deployment. Leave unset for US.
# 'eu' -> *.eu.soniox.com, 'jp' -> *.jp.soniox.com.
# The /api/get-temporary-api-key route echoes this value to the browser so
# the client SDK connects to the matching regional endpoints.
# SONIOX_REGION=eu
